const minutesToMilliseconds = (minutes: number) => minutes * 60 * 1000

const secondsToMilliseconds = (seconds: number) => seconds * 1000

export default { minutesToMilliseconds, secondsToMilliseconds }
